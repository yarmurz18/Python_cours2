name: tests

on:
  pull_request:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        run: echo 'Started test"
      - name: Get info
        run: |
          echo: 'Getting comp info...'
          whoami
          sleep 5
          pwd










